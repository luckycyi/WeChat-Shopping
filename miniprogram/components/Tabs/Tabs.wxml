<view class="tabs_container">
  <view class="tabs_title">
    <view class="tabs_item {{item.isActive ? 'active' : ''}}" wx:for="{{tabs}}" wx:key="id" bindtap="handleTapItem" data-index="{{index}}">
    {{item.value}}
  </view>
  </view>
  <view class="container_item">
    <slot></slot>
  </view>
</view>
